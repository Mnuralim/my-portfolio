import React from 'react'
import Form from './components/Form'

const Page = () => {
  return (
    <section className="h-screen fixed z-20 bg-hard-color w-full top-0 left-0 flex items-center justify-center">
      <Form />
    </section>
  )
}

export default Page
